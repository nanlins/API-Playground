[tool.ruff]
target-version = "py313"
line-length = 120
exclude = [".venv", "venv", "__pycache__", ".pytest_cache", ".ruff_cache"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]
ignore = ["BLE001"]
